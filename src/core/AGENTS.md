# AGENTS.md

## 역할
- `src/core`는 그래프 모델과 순수 정규화 로직을 담당한다.

## 작업 규칙
- React, DOM, fetch, editor, 플러그인 의존 코드를 넣지 않는다.
- 입력과 출력이 명확한 순수 함수/순수 데이터 중심으로 유지한다.
- 의미가 바뀌는 변경은 타입과 테스트를 먼저 바꾼다.

## 확인
- `tests/core`를 가장 먼저 본다.
