# AGENTS.md

## 역할
- React 데모의 실제 앱 소스다.

## 작업 규칙
- `entities`, `features`, `pages`, `widgets`, `shared` 경계를 유지해 샘플 구조가 읽히게 만든다.
- `app.tsx`의 mock API 엔드포인트는 request 추적 데모의 일부이므로 조심해서 바꾼다.
- 데모는 설명 가능한 복잡도를 유지한다. 과도한 상태 관리나 라이브러리 실험 코드를 쌓지 않는다.
