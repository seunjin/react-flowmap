# AGENTS.md

## 역할
- `scripts/`는 빌드 보조나 문서화 보조 같은 저장소 지원 스크립트를 둔다.

## 작업 규칙
- 스크립트는 저장소 루트 기준으로 동작하게 유지한다.
- 가능하면 재실행해도 안전한 idempotent 형태로 만든다.
- 스크립트가 생성 파일을 만들면 소스 파일과 산출물의 관계를 상위 `AGENTS.md`나 README에 남긴다.
- 애플리케이션 로직이나 런타임 상태는 여기로 끌어오지 않는다.

## 확인
- 스크립트를 바꿨다면 실제 대상 파일이 기대한 위치로 생성되는지 확인한다.
